{
  "engines": {
    "node": "20",
    "npm": ">=9",
    "pnpm": ">=8"
  },
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces",
    "start": "npm run start --workspace back"
  },
  "devDependencies": {
    "prettier": "^3.1.1"
  },
  "workspaces": [
    "back",
    "front"
  ]
}
