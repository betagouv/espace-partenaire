{
  "engines": {
    "node": "20.10.0",
    "npm": ">=9",
    "pnpm": ">=8"
  },
  "private": true,
  "scripts": {
    "build": "npm run build --workspaces",
    "start": "npm run start --workspace back"
  },
  "devDependencies": {
    "prettier": "^3.1.0"
  },
  "workspaces": [
    "back",
    "front"
  ]
}
